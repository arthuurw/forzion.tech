<!--
Este arquivo é mantido pela skill "maf-agent-guardian".
⚠️ NUNCA registre API keys, secrets, ou conteúdo de prompts com PII aqui.
-->

# Memória de Segurança de Agentes

**Projeto:** Forzion Tech IA
**Stack MAF:** Microsoft.Agents.AI 1.6.1 / .NET 8
**Provedor LLM:** Claude Haiku-4.5 via Anthropic API (ver DC-001)
**Última revisão:** 2026-05-16

---

## Agentes registrados

> Todo agente em produção precisa estar nesta seção. Agente não listado = agente shadow = bloqueador para release.

### AlunoAssistant _(planejado — Sprint 2)_

- **Tipo:** Interno
- **Endpoint:** `POST /aluno/assistant/chat`
- **Auth:** `[Authorize("Aluno")]` — JWT claim `tipo_conta=Aluno`
- **Modelo:** `claude-haiku-4-5-20251001` / Temperature: 0.3 / Max tokens: 800
- **Owner técnico:** A definir
- **Threat model:** `docs/threat-models/agente-aluno.md` _(a criar no Sprint 2)_
- **Tools:**

  | Tool | Tier | Approval | Validador |
  |------|------|----------|-----------|
  | `get_meus_treinos` | Read | — | AlunoId closure = JWT alunoId |
  | `get_historico_execucoes` | Read | — | AlunoId closure = JWT alunoId |
  | `get_proximo_treino` | Read | — | AlunoId closure = JWT alunoId |
  | `get_detalhe_exercicio` | Read | — | ExercicioId format + existência |

- **Guard rails planejados:** input regex | unicode normalize | output PII scan | markdown sanitize | rate limit | token quota | OTel
- **Quotas planejadas:**
  - Tokens/usuário/dia: 50.000
  - Tool calls/sessão: 15
  - Requests/hora/user: 20

---

### TreinadorAssistant _(planejado — Sprint 3)_

- **Tipo:** Interno
- **Endpoint:** `POST /treinador/assistant/chat`
- **Auth:** `[Authorize("Treinador")]` — JWT claim `tipo_conta=Treinador`
- **Modelo:** `claude-haiku-4-5-20251001` / Temperature: 0.3 / Max tokens: 1200
- **Owner técnico:** A definir
- **Threat model:** `docs/threat-models/agente-treinador.md` _(a criar no Sprint 3)_
- **Tools:**

  | Tool | Tier | Approval | Validador |
  |------|------|----------|-----------|
  | `get_meus_alunos` | Read | — | TreinadorId closure = JWT treinadorId |
  | `get_progresso_aluno` | Read | — | Vínculo ativo TreinadorId↔AlunoId |
  | `get_fichas_aluno` | Read | — | Vínculo ativo TreinadorId↔AlunoId |
  | `get_execucoes_recentes_aluno` | Read | — | Vínculo ativo TreinadorId↔AlunoId |
  | `sugerir_ficha_treino` | **Write** | **Obrigatório (preview)** | Vínculo ativo + formato válido dos parâmetros |

- **Guard rails planejados:** input regex | unicode normalize | output PII scan | markdown sanitize | rate limit | token quota | OTel
- **Quotas planejadas:**
  - Tokens/usuário/dia: 100.000
  - Tool calls/sessão: 15
  - Requests/hora/user: 10

---

## Decisões arquiteturais conscientes

### DC-001 — Escolha de provider LLM ✅ _Decidido em 2026-05-16_

**Decisão:** Claude Haiku-4.5 (`claude-haiku-4-5-20251001`) via Anthropic API.

**Justificativa:** qualidade em PT-BR superior a modelos locais; custo baixo (~$0.002/request com prompt caching); OCI VM atual sem GPU tornaria Ollama lento demais para UX aceitável.

**Custo estimado:**
- ~$3/mês em early stage (~50 req/dia)
- ~$30/mês em 100 users ativos (~500 req/dia)
- ~$150/mês em 500 users (~2.500 req/dia)

**Risco LGPD aceito conscientemente:** dados físicos de alunos processados em servidores Anthropic (EUA). Mitigações obrigatórias antes de ir para produção:
- Verificar e confirmar que API usage da Anthropic não é usada para treino de modelos (por padrão API não é, mas deve ser verificado nos termos vigentes)
- Documentar base legal LGPD para transferência internacional (Art. 33)
- Não incluir CPF, nome completo, ou dados de saúde clínica no contexto do LLM

**Credenciais:** via variável de ambiente / User Secrets. Não hardcoded. Não incluídas ainda (Sprint 1).

### DC-002 — Sem agente externo (pesquisa web) na fase inicial

Agente de pesquisa externa de exercícios identificado como valor futuro, mas fora do escopo das Sprints 1-4. Registrado aqui para evitar criação de agente híbrido (interno + externo no mesmo agente).

---

## MCP servers / integrações de terceiros

| Servidor / Lib | URL/Origem | Tools usadas | Auth | Aprovação | Notas |
|-----|-----|-----|-----|-----|-----|
| — | — | — | — | — | Nenhuma integração MCP de terceiro planejada |

---

## Débitos de segurança pendentes

### 🔴 Crítico
- **F2:** Toda tool que aceita `AlunoId` como parâmetro no Agente B **deve** validar vínculo ativo `TreinadorId↔AlunoId` antes de retornar dados. Implementar em `TreinadorTools.cs` com query ao repositório de vínculos.
- **F3:** `sugerir_ficha_treino` **nunca** persiste diretamente. Deve retornar preview JSON. Persistência só ocorre via endpoint separado com confirmação explícita do usuário.

### 🟠 Alto
- **F4:** `InputNormalizer` (unicode) + `PromptInjectionPatterns` (regex) ausentes — implementar no Sprint 1.
- **F5:** `OutputScanner` ausente — implementar no Sprint 1.
- **F6:** Token quota por usuário/dia ausente — implementar no Sprint 1.
- **F8:** Antes de produção — documentar base legal LGPD Art. 33 + verificar termos Anthropic API sobre uso de dados para treino (ver DC-001).

### 🟡 Médio
- **F7:** OpenTelemetry não configurado no projeto — adicionar no Sprint 1.
- **F9:** Confirmar uso de `ManagedIdentityCredential` em produção (não `DefaultAzureCredential`).
- **F10:** `AgentRegistry` a implementar no Sprint 1 para evitar agentes shadow.

### 🟢 Baixo
- Considerar `SharpToken` para estimativa exata de tokens (em vez de `length/4`).
- Avaliar topic boundary check para restringir perguntas fora do domínio fitness/treino.

---

## Incidentes / Near-misses

- _(nenhum — projeto em fase de planejamento)_

---

## Histórico de revisões

### 2026-05-16 — Revisão #1

- **Agentes revisados:** planejamento inicial (AlunoAssistant, TreinadorAssistant)
- **Achados:** 🔴 2 / 🟠 4 / 🟡 3 / 🟢 2
- **Aplicados:** 0 (pré-implementação)
- **Adiados:** 0
- **Decisões conscientes adicionadas:** 2 (DC-001, DC-002)
- **Foco:** threat modeling e planejamento de sprints

**Validações de documentação realizadas:**
- Microsoft.Agents.AI versão estável: `1.6.1` (NuGet, verificado 2026-05-16)
- Microsoft.Extensions.AI: incluído via dependência MAF
- Mudanças MAF desde 1.0 GA: WebBrowsingTool allow-listing nativo (1.5.0); OTel auto-wiring breaking change (1.6.1); Magentic Orchestration ainda experimental
- APIs validadas: `ChatClientAgentOptions`, `AIFunctionFactory.Create`, `WebBrowsingTool` (allow-listing), `HostedMcpServerToolApprovalMode.AlwaysRequire`
- Advisories de segurança consultados: nenhum CVE encontrado nos releases recentes
- Divergências entre snapshot da skill e doc atual: skill referenciava `1.0.*`, versão estável atual é `1.6.1`

---

## Notas para próxima revisão

- DC-001 resolvido ✅ — Claude Haiku-4.5 via Anthropic API
- Revisar esses achados após Sprint 1 estar implementado
- Próxima revalidação de documentação oficial: até 2026-08-16 (trimestral) ou quando sair release MAF 1.7+
