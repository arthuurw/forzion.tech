{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing — src/lib + src/hooks + src/components (F8 expansao Fase 3). Roda semanal via GH Actions, nao em PR. break baixou de 75 -> 60 pra dar runway aos componentes ate baseline ser medido; ratchet de volta apos 2-3 runs estabilizarem.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.config.mts"
  },
  "checkers": ["typescript"],
  "tsconfigFile": "tsconfig.json",
  "mutate": [
    "src/lib/**/*.ts",
    "src/hooks/**/*.ts",
    "src/components/**/*.tsx",
    "src/components/**/*.ts",
    "!src/**/*.test.*",
    "!src/**/*.test.ts",
    "!src/**/*.test.tsx",
    "!src/**/*.property.test.ts",
    "!src/**/*.stories.*",
    "!src/**/*.d.ts",
    "!src/test/**",
    "!src/lib/api/client.ts",
    "!src/lib/auth/context.tsx"
  ],
  "ignorePatterns": [
    "node_modules",
    ".next",
    "coverage",
    "playwright-report",
    "test-results",
    "storybook-static",
    "e2e",
    ".stryker-tmp",
    "reports"
  ],
  "thresholds": {
    "high": 85,
    "low": 75,
    "break": 60
  },
  "reporters": ["html", "clear-text", "progress"],
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  },
  "coverageAnalysis": "perTest",
  "timeoutMS": 60000,
  "concurrency": 4,
  "dryRunTimeoutMinutes": 5,
  "tempDirName": ".stryker-tmp",
  "cleanTempDir": "always"
}
