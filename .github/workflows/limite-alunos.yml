name: Limite de Alunos (Graça)

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  processar-limite-alunos:
    name: Graça de limite de alunos (carimbo, lembretes e apara)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: ${{ fromJSON(vars.CRON_ENVIRONMENTS || '["homolog"]') }}
    environment: ${{ matrix.env }}
    steps:
      - name: Conecta no tailnet
        uses: tailscale/github-action@780049a30b6ff5c378a9e7b389d15ece7a204888 # v4.1.3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
          version: latest

      - name: Processa graça de limite de alunos
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2 # v1
        with:
          host: ${{ secrets.VM_TAILNET_IP }}
          username: ubuntu
          key: ${{ matrix.env == 'production' && secrets.PROD_SSH_KEY || secrets.HOMOLOG_SSH_KEY }}
          script: |
            set -eu
            cd ${{ vars.APP_DIR || '/opt/forzion/app' }}
            EF=${{ vars.ENV_FILE || '/opt/forzion/.env' }}
            PROJ="${{ vars.COMPOSE_PROJECT }}"
            DC="docker compose ${PROJ:+-p $PROJ} -f ${{ vars.COMPOSE_FILE || 'docker-compose.homolog.yml' }} --env-file $EF"
            KEY="$(grep -E '^INTERNAL_API_KEY=' "$EF" | head -1 | cut -d= -f2-)"
            $DC exec -T backend curl -fsS -X POST \
              -H "X-Internal-Key: $KEY" -H "Content-Type: application/json" \
              "http://localhost:8080/internal/processar-limite-alunos"

      - name: Healthchecks ping (success)
        if: success()
        run: curl -fsS -m 10 --retry 3 "https://hc-ping.com/${{ secrets.HC_PING_KEY }}/limite-alunos" || true
      - name: Healthchecks ping (failure)
        if: failure()
        run: curl -fsS -m 10 --retry 3 "https://hc-ping.com/${{ secrets.HC_PING_KEY }}/limite-alunos/fail" || true

      - name: Notificar falha via issue
        if: failure()
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          script: |
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const title = `[limite-alunos] Falha em ${new Date().toISOString().slice(0, 10)}`;
            const body = [
              '⚠️ Workflow **Limite de Alunos (Graça)** falhou.',
              '',
              `**Run:** ${runUrl}`,
              `**Trigger:** ${context.eventName} (${context.ref})`,
              '',
              'Sem o processamento diário: carimbos de início de graça, lembretes (D-30/D-7/D-1) e a',
              'apara de vínculos excedentes ao fim da janela de 3 meses (GRACE-05) não avançam hoje.',
              '',
              'Investigar (ordem dos layers):',
              '1. Tailnet: runner conectou? ACL `tag:ci -> VM:22` (SSH) liberada?',
              '2. SSH: `VM_TAILNET_IP` bate com a VM? Chave SSH do ambiente `${{ matrix.env }}` válida?',
              '3. `docker compose exec backend` rodou? `/internal/processar-limite-alunos` respondeu (401=key, 5xx=backend)?',
              '4. Backend logs (`docker compose logs backend --tail 200`)',
            ].join('\n');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body,
              labels: ['ops', 'limite-alunos-failed']
            });
