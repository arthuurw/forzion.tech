version: 2
updates:
  - package-ecosystem: "nuget"
    directory: "/"
    target-branch: "homolog"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    open-pull-requests-limit: 5
    groups:
      nuget-minor-patch:
        update-types:
          - "minor"
          - "patch"
    ignore:
      # >10.28.0.143324 trava `dotnet format --verify-no-changes` na CI (Linux) com
      # exit code 2 (bug do analyzer, nao reproduz no Windows local) -- ver #354/#357/#359.
      # Remover quando a Sonar publicar uma versao que nao quebre o format.
      - dependency-name: "SonarAnalyzer.CSharp"
        versions: [">10.28.0.143324"]
    commit-message:
      prefix: "chore"

  - package-ecosystem: "npm"
    directory: "/frontend"
    target-branch: "homolog"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    open-pull-requests-limit: 5
    groups:
      npm-minor-patch:
        update-types:
          - "minor"
          - "patch"
    ignore:
      # TS 7 (reescrita nativa/Go) quebra API interna do compiler usada por
      # typescript-eslint e madge (`Cannot read properties of undefined (reading 'Cjs')`)
      # -- ver #345. Remover quando o ecossistema (typescript-eslint/madge) suportar TS7.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
    commit-message:
      prefix: "chore"

  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "homolog"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    open-pull-requests-limit: 5
    groups:
      actions-minor-patch:
        update-types:
          - "minor"
          - "patch"
    commit-message:
      prefix: "chore"

  # Propõe bumps de digest das imagens pinadas @sha256 nos compose (pact-broker,
  # certbot). NÃO alcança imagens pinadas dentro de .github/workflows/**
  # (semgrep.yml `container.image`, zap.yml/ci.yml `docker run` inline) — o
  # Dependabot ainda não suporta isso (dependabot-core#5819, em aberto); essas
  # duas exigem bump manual, travado por scripts/test/ci-hygiene-check.sh.
  - package-ecosystem: "docker-compose"
    directory: "/"
    target-branch: "homolog"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore"
